= Session 024 — Option C : assembleCompositeContext dans runner-gradle
:session: 024
:date: 2026-05-23
:type: feat
:status: TERMINEE
:score: 9/10
:projet: runner-gradle (New Jersey N3) + workspace-gradle/pilot-plugin (South Central N4)

== Résumé

Implémentation d'`AssembleCompositeContextTask` dans le pilot-plugin (workspace-gradle N4).
La tâche orchestre les 3 boroughs producteurs (codex N2 + training N2 + codebase N1)
via ProcessBuilder et fusionne leurs résultats en un `composite-context.json` unifié.

Le runner reste à 4 lignes (`plugins { bakery, pilot }`). La logique métier
d'assemblage est dans le pilot-plugin, pas dans le terminal consommateur.

== Fichiers créés

|===
| Fichier | Rôle
| `pilot-plugin/src/main/kotlin/pilot/AssembleCompositeContextTask.kt` | 142 lignes — orchestration codex→training→codebase via ProcessBuilder, fusion JSON, metadata.json
|===

== Fichiers modifiés

|===
| Fichier | Modification
| `pilot-plugin/src/main/kotlin/pilot/PilotExtension.kt` | +3 propriétés (codexProjectDir, trainingProjectDir, codebaseProjectDir)
| `pilot-plugin/src/main/kotlin/pilot/PilotPlugin.kt` | +enregistrement assembleCompositeContext (groupe context, conventions, output build/composite-context.json)
| `pilot-plugin/src/main/kotlin/pilot/InfoTask.kt` | +groupe "context" dans pluginGroups
|===

== Architecture

----
./gradlew assembleCompositeContext (runner)
  └── pilot-plugin → AssembleCompositeContextTask
       ├── ProcessBuilder → codex-gradle/generateCompositeContext
       │    → build/codex/composite-context.json
       ├── ProcessBuilder → training-gradle/generateCompositeContext
       │    → build/training/composite-context.json
       └── ProcessBuilder → codebase-gradle/generateCompositeContext
            → build/codebase/composite-context.json
  → build/composite-context.json (unifié)
----

== Pipeline

Pipeline A→C→B complété :
- Option A : inferMetadata (session 006) — ✅ TERMINE
- Option C : assembleCompositeContext (session 024) — ✅ TERMINE
- Next : EPIC L koog+langchain4j

== Build

- pilot-plugin compileKotlin + test + publishToMavenLocal : BUILD SUCCESSFUL
- runner-gradle ./gradlew info : BUILD SUCCESSFUL
- ./gradlew assembleCompositeContext : BUILD SUCCESSFUL
  - codex-gradle : exit=1 (conflit org.jetbrains:annotations préexistant)
  - training-gradle : exit=1 (plugin readme non résolu, préexistant)
  - codebase-gradle : exit=1 (libs.plugins non résolus, préexistant)
  - Résultat : composite-context.json avec 0 entries (boroughs cassés mais tâche résiliente)

== Leçon

Les 3 boroughs N1/N2 ont des problèmes de compilation préexistants (documentés session 020).
À traiter dans leurs propres sessions. L'orchestration fonctionne — la tâche est déterministe
et résiliente (0 entry = pas d'échec de build).
