= PROMPT_REPRISE — runner-gradle
:session: 027
:date: 2026-06-12
:etat: Runner = bakery 0.0.1 (Maven Central) — configPath multi-site (-PsiteName). publishSite déplacé dans bakery (N2). CI magic-stick + cheroliv.com OK.
:epic_actif: Aucun — next = WSM-3 pilot-plugin tests OU EPIC L koog

== Contexte de fin

Runner-gradle build.gradle.kts :
----
plugins {
    id("education.cccp.bakery") version "0.0.1"
}

val siteName: String = project.findProperty("siteName") as String? ?: throw ...
val resolvedConfigPath = file("${System.getenv("OFFICE_PATH") ?: ...}/sites/$siteName/site.yml").absolutePath

bakery { configPath = resolvedConfigPath }
----

settings.gradle.kts : mavenLocal() + mavenCentral() + gradlePluginPortal()

== Reprendre à

. `git status`
. `./gradlew tasks -PsiteName=magic-stick` — confirmer deploySite + publishSite (provenant bakery) présent
. WSM-3 — Tests pilot-plugin
. OU EPIC L — Migration langgraph4j → koog+langchain4j

== ✅ EPIC MEM-CAT-ROLLOUT — Adoption version catalog workspace (S-028, cross-borough MEMPHIS)

_Origine : demande pilote S-028 (workspace-bom) — cas C7 du cadrage MEM-CATALOG
(hardcode `id(...) version` non couvert). Référentiel : `workspace-bom/BACKLOG.adoc`._

=== US-ROLLOUT-5 — Pin workspace-catalog + alias bakery (TERMINÉ S-031)

*Statut* : ✅ TERMINÉ (S-031)

- [x] `settings.gradle.kts` : `dependencyResolutionManagement { repositories { mavenLocal(); mavenCentral() } + create("ws") from(...0.0.31) }` (pin unique D4)
  — piège : le runner n'avait PAS de `dependencyResolutionManagement.repositories`
  (que pluginManagement) → "no repositories are defined" au 1er essai, corrigé.
- [x] Entrée `[plugins]` bakery ajoutée au toml workspace-bom (D7) + release BOM+catalog 0.0.31 (mavenLocal).
- [x] `build.gradle.kts` : `id("education.cccp.bakery") version "0.0.9"` → `alias(ws.plugins.bakery)` (hardcode C7 éliminé).
- [x] Gate : `./gradlew tasks -PsiteName=magic-stick` VERT (deploySite + publishSite présents)
  + `buildEnvironment` prouve `education.cccp:bakery-plugin:0.0.9` résolu via ws.
- [ ] CI magic-stick/cheroliv.com : déclencher un run réel pour confirmation deploy.

== Fichiers touchés

- runner-gradle/build.gradle.kts → -PsiteName dynamique + configPath hors DSL
- runner-gradle/.agents/sessions/026-fix-bakery-multisite.adoc → nouveau
- office/.github/workflows/deploy-magic-stick.yml → cccp-education/runner-gradle
