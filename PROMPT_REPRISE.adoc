= PROMPT_REPRISE — runner-gradle
:session: 026
:date: 2026-06-12
:etat: Runner = bakery 0.0.1 (Maven Central) + publishSite task. CI cheroliv.com OK.
:epic_actif: Aucun — next = WSM-3 pilot-plugin tests OU EPIC L koog

== Contexte de fin

Runner-gradle build.gradle.kts :
----
plugins {
    id("education.cccp.bakery") version "0.0.1"
}

bakery { configPath = file("${System.getenv("OFFICE_PATH") ?: ...}/sites/cheroliv.com/site.yml").absolutePath }

tasks.register("publishSite") {
    group = "publish"
    description = "Bake + deploy cheroliv.com"
    dependsOn("bake", "deploySite")
}
----

settings.gradle.kts : mavenLocal() + mavenCentral() + gradlePluginPortal()

== Reprendre à

. `git status`
. `./gradlew info` — confirmer runner v2.0
. WSM-3 — Tests pilot-plugin
. OU EPIC L — Migration langgraph4j → koog+langchain4j

== Fichiers touchés

- runner-gradle/build.gradle.kts → bakery 0.0.1 + publishSite
- runner-gradle/settings.gradle.kts → +mavenCentral()
- runner-gradle/.agents/sessions/025-fix-bakery-publishSite-ci.adoc → nouveau
