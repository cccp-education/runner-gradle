= PROMPT_REPRISE — runner-gradle
:session: 021
:date: 2026-05-23
:etat: Cleanup pilot-plugin TERMINE — verifyBoroughs delegue a workspace-gradle N4
:epic_actif: N/A

== Contexte de fin

verifyBoroughs (+ findFileRecursive, findExtensionRegistration, inferMetadataFromFilesystem,
extractAdocTitle, BoroughDef preBootstrapBoroughs) supprime du build.gradle.kts (-364 lignes).
La logique vit maintenant dans le pilot-plugin de workspace-gradle (foundry/private/workspace-gradle/pilot-plugin/).

Runner build.gradle.kts : 577 → 213 lignes.
Objectif <200 lignes atteignable avec generateApiSchema → workspace-gradle.

== Reprendre à

. `./gradlew info` — verifier Runner v2.0
. `git status` — voir les fichiers modifies non commites
. Prochaine session : deleguer generateApiSchema (~50 lignes) → workspace-gradle (N4)
  - Objectif : build.gradle.kts < 200 lignes (plugins + bakery + dagLevels + info + deployWorkspace)

== Fichiers touches

- `build.gradle.kts` — -364 lignes (577→213), verifyBoroughs + helpers supprimes, +pilot plugin 0.0.0
- `foundry/private/workspace-gradle/pilot-plugin/` — NOUVEAU sous-plugin N4
  - `PilotPlugin.kt` — 15 lignes
  - `VerifyBoroughsTask.kt` — 250 lignes (logique complete)
  - `BoroughDef.kt` — 15 lignes (dataclass)
  - `PilotExtension.kt` — 17 lignes
  - `build.gradle.kts` — 65 lignes (plugin publish)
  - `gradle/libs.versions.toml` — 39 lignes

== Compilation — Statut

- runner-gradle → ✅ BUILD SUCCESSFUL (info + verifyBoroughs via pilot plugin)
